TODO
при відобр фону нагрівача прозорість в залежності від ступіню нагрівання.
розмір вантажу у двох вимірах
 

побудувати цикл парового двигуна

кпд цикла

            
             ----------------------------------------------------------------------------------------------
                  
       


            // let dv = (wanted * 200) / 2 * Math.sqrt(2 * initP / (this.plunger.volume * 10000 * 0.4));
            // let v = Math.sqrt(2 * initP * this.plunger.volume / (10000 * 0.4));       // sqrt(2PV/mn)
            // let eps_v = dv / v; 